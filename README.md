# Finding-the-Next-Valid-Four-Digit-Number-with-All-Distinct-Digits
This solution incrementally checks each number after the given input until it finds the first four-digit number in which every digit is unique. By using a set to verify digit uniqueness, the algorithm remains simple, readable, and efficient.
